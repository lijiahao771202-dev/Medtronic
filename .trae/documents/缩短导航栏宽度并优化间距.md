## 方案
- 将导航整体最大宽度改短：`max-width: clamp(560px, 72vw, 720px)`（桌面约 720px，随屏幕自适应）。
- 内部间距与圆角微调：`padding: 10px 14px`、`gap: 10px`、`border-radius: 20px`，链接胶囊维持液态玻璃风格。
- 移动端保持 92vw 宽度，便于点击。

## 文件与修改点（仅样式）
- `src/App.css`：更新 `.navbar-wrap` 与 `.navbar` 的 `max-width/padding/gap/border-radius`，保持现有风格与交互。

## 验收
- 导航栏更短更紧凑，居中对齐；液态玻璃质感保持一致；移动端正常显示。