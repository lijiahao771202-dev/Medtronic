## 设计调整
- 保留整条导航栏的玻璃背板（与首页/语音聊天一致的液态玻璃渐变、模糊、内外阴影、细边框）。
- 仅当前激活的导航项显示“玻璃胶囊”内层容器；非激活项为简洁文字，hover 微亮。

## 样式要点
- `.navbar`（整体背板）：
  - 保持现有液态玻璃背景与边框阴影
  - 居中限宽 `max-width: clamp(560px, 72vw, 720px)`、`padding: 10px 14px`、`gap: 10px`、`border-radius: 20px`
- `.navbar a`（默认非激活）：
  - 文字颜色 `var(--text-secondary)`、`padding: 8px 12px`、`border-radius: 14px`、`background: transparent`、`hover` 微亮
- `.navbar a.active`（激活项）：
  - 胶囊玻璃：`border: 1px solid rgba(255,255,255,0.30)`、`background: radial+linear 渐变`、`backdrop-filter: blur(16px) saturate(140%)`、`box-shadow: inset 0 1px rgba(255,255,255,0.35), 0 20px 60px rgba(12,40,62,0.35)`、`border-radius: 18px`、`padding: 10px 14px`

## 文件与改动
- `src/App.css`：保留 `.navbar` 背板；调整 `.navbar a` 默认为透明；`.navbar a.active` 应用玻璃胶囊。
- `src/App.jsx`：不改结构（继续使用 `.navbar-wrap` + `.navbar`）。

## 验收
- 导航整体为玻璃背板；只有当前页面的链接项为玻璃胶囊；其他项为简洁文字；移动端正常显示。