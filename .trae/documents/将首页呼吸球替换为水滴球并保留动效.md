## 目标
- 将首页中间的“呼吸球”替换为“水滴球（曾用名：呼吸球）”，外观与参考图一致。
- 完全保留现有呼吸动效（吸气/屏息/呼气的平滑缩放与轻微抖动）。
- 视觉符合“液态玻璃”标准：通透发亮、边缘厚度、磨砂与轻微色散，交互弹性保留。

## 实现方式
1. 组件改造
- 在现有动效逻辑基础上重用 `scale` 动画（`src/components/BreathingBall.jsx:10–58`）。
- 将渲染由纯圆形改为 SVG 水滴形：在同组件内新增 `variant="drop"` 支持，默认按水滴渲染。
- 或创建新组件 `WaterDropBall.jsx` 并迁移动效逻辑为复用的钩子（如 `useBreathingScale`）。推荐新组件以便样式隔离与别名注释。

2. SVG 水滴外观（与参考图一致）
- 使用单一 `<path>` 构造标准水滴：顶部圆弧 + 底部尖点，路径按 200×200 视口居中。
- 填充层次：
  - 基底填充：径向渐变（中心高亮到边缘半透明），颜色中性偏暖以匹配参考图。
  - 高光覆盖：顶部左侧椭圆高光（径向渐变+模糊）。
  - 边缘厚度：描边 1.5–2px 半透明白，并加内发光（内阴影+描边叠加）。
  - 轻微色散：三色边缘影（R/G/B 低不透明度的 `drop-shadow`）以制造通透发亮的彩边。
- 文本层（分钟或阶段文案）保持中心显示，随 `scale` 变化。

3. 动效保留与弹性
- `scale` 完全沿用现有阶段节奏；`hold` 阶段保留轻微抖动（`sin` 抖动）。
- 鼠标悬停不再改变亮度（已改为常驻通透）；保留轻微位移弹性可选。

4. 样式隔离
- 为水滴球新建 CSS Modules：`src/components/WaterDropBall.module.css`，容器与文本样式在模块内，避免污染全局。
- 删除或停用 `App.css` 中的 `.ball` 相关样式对新组件的影响（保持兼容旧球体时不删除）。

5. 集成与替换
- 在 `src/pages/Home.jsx:65` 将 `BreathingBall` 替换为 `WaterDropBall`，传入 `running`, `minutes`, `showSliderValue`。
- 组件顶部注释加入别名追踪：`/** @description 水滴球（曾用名：呼吸球）... */`。

6. 验证
- 桌面与移动端（≤640px）检查比例与通透感，确保响应式良好。
- 运行时阶段切换检查 `scale` 行为与文案显示。
- 对比参考图：
  - 水滴形状、高光位置与强度、边缘厚度与彩边一致。
  - 玻璃磨砂通透常驻，无需悬停触发。

## 变更清单
- 新增：`src/components/WaterDropBall.jsx`、`src/components/WaterDropBall.module.css`
- 修改：`src/pages/Home.jsx` 替换组件引用与使用
- 可选：提取动效钩子 `useBreathingScale` 以便复用

## 预计影响范围
- 首页渲染水滴球取代原球体，其他页面不受影响。
- 保持现有状态流与交互不变（开始/结束冥想、时长显示）。

## 允许微调项（按你确认后执行）
- 色温与高光强度（更偏暖或偏冷以贴图）
- 水滴尖端锐度与圆弧比例（视觉更接近参考图）
- 文本字号与权重（对齐参考图强调程度）

请确认以上方案；确认后我将开始实施并展示效果对比。