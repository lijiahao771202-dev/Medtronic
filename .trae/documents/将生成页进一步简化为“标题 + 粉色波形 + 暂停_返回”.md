## 调整点
- 移除玻璃态容器与多余按钮，只保留居中标题、粉色时间域波形、两个按钮（暂停/返回）。
- “提示词”仅保留一个轻量按钮，点击后展开编辑区与“用此提示词生成”，默认收起。
- 波形改为连续折线（时间域），风格与示例一致，画布透明、无边框。

## 代码修改
- `src/pages/GenerateScript.jsx`
  - 删除外层玻璃卡片容器，改为简洁居中布局。
  - 移除“重新生成”主按钮，保留暂停/返回。
  - 使用 `getByteTimeDomainData` 绘制粉色折线波形；画布宽度按容器自适应。
- `src/App.css`
  - 将 `.waveform` 背景与边框改为透明；居中显示；标题样式更简洁。

## 验收
- 页面只呈现标题、粉色波形和两个按钮；提示词通过按钮展开编辑；整体视觉更简洁，与提供示例一致。